#include <bits/stdc++.h>
using namespace std;
 

using ll = long long ;
using i128 = __int128;
using ld  = long double;
using u32 = unsigned int ;
using u64 = unsigned long long ;
using vl = vector<ll>;
using pl = pair<ll,ll>;
using vp = vector<pl>;

ll mod=1000000007;


#define f(i, a, b, c) for(ll i = a; i < b; i += c)


#define fd(i, a, b, c) for(ll i = a; i > b; i -= c)

#define pb push_back
#define mp make_pair
#define ss second
#define ff first
#define all(a) a.begin(),a.end()

vl shortest_path(vp edges,ll n,ll s,llt)





void solve(){
   
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    #ifndef ONLINE_JUDGE
        freopen("input.txt","r",stdin);
        freopen("output.txt","w",stdout);
    #endif

    u32 t=1;
    cin>>t;
    while(t--){
        solve();
    }
    return 0;
    
}